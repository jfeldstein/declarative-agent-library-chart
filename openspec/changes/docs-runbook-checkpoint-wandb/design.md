# Runbook and operator docs

See **`checkpointing-observability-delivery-plan/design.md`** for full ordering (step **11** / **13**).

**Merged baseline:** `main` @ PR #11.

**Scope notes:** Implementations live under `runtime/src/hosted_agents/` (and Helm/docs for steps 10–11). Tests live under `runtime/tests/`.
