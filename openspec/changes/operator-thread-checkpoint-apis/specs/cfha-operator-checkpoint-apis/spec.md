# Spec: Operator HTTP APIs for threads

### Requirement: Slice 4 boundaries documented

The project SHALL treat **step 4 of 13** as defined in `checkpointing-observability-delivery-plan/design.md` and this change’s `proposal.md`.

#### Scenario: Traceability

- **WHEN** a developer looks for scope of operator http apis for threads
- **THEN** they SHALL find it under `openspec/changes/operator-thread-checkpoint-apis/`
