# Spec: ATIF v1.4 export

### Requirement: Slice 7 boundaries documented

The project SHALL treat **step 7 of 13** as defined in `checkpointing-observability-delivery-plan/design.md` and this change’s `proposal.md`.

#### Scenario: Traceability

- **WHEN** a developer looks for scope of atif v1.4 export
- **THEN** they SHALL find it under `openspec/changes/atif-v1-4-export/`
