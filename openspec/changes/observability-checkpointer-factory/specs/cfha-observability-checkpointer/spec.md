# Spec: Checkpointer factory (memory / none)

### Requirement: Slice 2 boundaries documented

The project SHALL treat **step 2 of 13** as defined in `checkpointing-observability-delivery-plan/design.md` and this change’s `proposal.md`.

#### Scenario: Traceability

- **WHEN** a developer looks for scope of checkpointer factory (memory / none)
- **THEN** they SHALL find it under `openspec/changes/observability-checkpointer-factory/`
