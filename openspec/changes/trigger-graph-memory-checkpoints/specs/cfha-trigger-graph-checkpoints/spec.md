# Spec: Trigger graph + memory checkpoints

### Requirement: Slice 3 boundaries documented

The project SHALL treat **step 3 of 13** as defined in `checkpointing-observability-delivery-plan/design.md` and this change’s `proposal.md`.

#### Scenario: Traceability

- **WHEN** a developer looks for scope of trigger graph + memory checkpoints
- **THEN** they SHALL find it under `openspec/changes/trigger-graph-memory-checkpoints/`
