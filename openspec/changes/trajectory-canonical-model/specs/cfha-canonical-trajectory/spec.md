# Spec: Canonical trajectory model

### Requirement: Slice 6 boundaries documented

The project SHALL treat **step 6 of 13** as defined in `checkpointing-observability-delivery-plan/design.md` and this change’s `proposal.md`.

#### Scenario: Traceability

- **WHEN** a developer looks for scope of canonical trajectory model
- **THEN** they SHALL find it under `openspec/changes/trajectory-canonical-model/`
