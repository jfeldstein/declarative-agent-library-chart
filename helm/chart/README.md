# Declarative Agent Library (Helm)

This chart is consumed as a dependency from the example app charts under `examples/`. It is linted via **chart-testing** (`ct lint`) together with those examples; template behavior is covered by **helm-unittest** suites in the example charts (this library has no standalone `tests/` chart).
