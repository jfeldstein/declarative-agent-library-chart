apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "declarative-agent-library.fullname" . }}
  labels:
    {{- include "declarative-agent-library.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "declarative-agent-library.agentSelectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "declarative-agent-library.agentSelectorLabels" . | nindent 8 }}
      {{- if .Values.o11y.prometheusAnnotations.enabled }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: {{ .Values.service.port | quote }}
        prometheus.io/path: "/metrics"
      {{- end }}
    spec:
      containers:
        - name: agent
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8088
              protocol: TCP
          env:
            - name: HOSTED_AGENT_SYSTEM_PROMPT
              valueFrom:
                configMapKeyRef:
                  name: {{ include "declarative-agent-library.fullname" . }}-config
                  key: system-prompt
            {{- if .Values.chatModel }}
            - name: HOSTED_AGENT_CHAT_MODEL
              value: {{ .Values.chatModel | quote }}
            {{- end }}
            - name: HOSTED_AGENT_RAG_BASE_URL
              value: {{ include "declarative-agent-library.ragInternalBaseUrl" . | quote }}
            - name: HOSTED_AGENT_SUBAGENTS_JSON
              valueFrom:
                configMapKeyRef:
                  name: {{ include "declarative-agent-library.fullname" . }}-config
                  key: subagents.json
            - name: HOSTED_AGENT_SKILLS_JSON
              valueFrom:
                configMapKeyRef:
                  name: {{ include "declarative-agent-library.fullname" . }}-config
                  key: skills.json
            - name: HOSTED_AGENT_ENABLED_MCP_TOOLS_JSON
              valueFrom:
                configMapKeyRef:
                  name: {{ include "declarative-agent-library.fullname" . }}-config
                  key: enabled-mcp-tools.json
            {{- if .Values.o11y.structuredLogs.json }}
            - name: HOSTED_AGENT_LOG_FORMAT
              value: "json"
            {{- end }}
            {{- $pgUrl := or .Values.observability.postgresUrl .Values.observability.checkpoints.postgresUrl }}
            {{- if $pgUrl }}
            - name: HOSTED_AGENT_POSTGRES_URL
              value: {{ $pgUrl | quote }}
            {{- end }}
            {{- if .Values.observability.checkpoints.enabled }}
            - name: HOSTED_AGENT_CHECKPOINTS_ENABLED
              value: "true"
            - name: HOSTED_AGENT_CHECKPOINT_BACKEND
              value: {{ .Values.observability.checkpoints.backend | quote }}
            {{- end }}
            {{- if .Values.observability.wandb.enabled }}
            - name: HOSTED_AGENT_WANDB_ENABLED
              value: "true"
            {{- if .Values.observability.wandb.project }}
            - name: WANDB_PROJECT
              value: {{ .Values.observability.wandb.project | quote }}
            {{- end }}
            {{- if .Values.observability.wandb.entity }}
            - name: WANDB_ENTITY
              value: {{ .Values.observability.wandb.entity | quote }}
            {{- end }}
            {{- end }}
            {{- if .Values.observability.slackFeedback.enabled }}
            - name: HOSTED_AGENT_SLACK_FEEDBACK_ENABLED
              value: "true"
            - name: HOSTED_AGENT_SLACK_EMOJI_LABEL_MAP_JSON
              valueFrom:
                configMapKeyRef:
                  name: {{ include "declarative-agent-library.fullname" . }}-config
                  key: slack-emoji-label-map.json
            {{- end }}
            {{- if .Values.observability.atifExport.enabled }}
            - name: HOSTED_AGENT_ATIF_EXPORT_ENABLED
              value: "true"
            {{- end }}
            {{- if .Values.observability.shadow.enabled }}
            - name: HOSTED_AGENT_SHADOW_ENABLED
              value: "true"
            - name: HOSTED_AGENT_SHADOW_SAMPLE_RATE
              value: {{ .Values.observability.shadow.sampleRate | quote }}
            {{- if .Values.observability.shadow.variantJson }}
            - name: HOSTED_AGENT_SHADOW_VARIANT_JSON
              value: {{ .Values.observability.shadow.variantJson | quote }}
            {{- end }}
            - name: HOSTED_AGENT_SHADOW_ALLOW_TENANTS_JSON
              valueFrom:
                configMapKeyRef:
                  name: {{ include "declarative-agent-library.fullname" . }}-config
                  key: shadow-allow-tenants.json
            {{- end }}
            {{- if .Values.observability.labelRegistry }}
            - name: HOSTED_AGENT_LABEL_REGISTRY_JSON
              valueFrom:
                configMapKeyRef:
                  name: {{ include "declarative-agent-library.fullname" . }}-config
                  key: label-registry.json
            {{- end }}
            {{- range .Values.extraEnv }}
            - name: {{ .name }}
              value: {{ .value | quote }}
            {{- end }}
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
