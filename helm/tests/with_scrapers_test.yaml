# Traceability: [DALC-REQ-HELM-UNITTEST-001] [DALC-REQ-RAG-SCRAPERS-002] [DALC-REQ-HELM-UNITTEST-004] [DALC-REQ-EXAMPLE-VALUES-FILES-001]
values:
  - ../../examples/with-scrapers/values.yaml
suite: with-scrapers default values
release:
  name: dalc-ci-scrapers
tests:
  - it: renders one ConfigMap per enabled jira and slack job
    template: charts/declarative-agent/templates/scraper-job-configmaps.yaml
    asserts:
      - hasDocuments:
          count: 3
  - it: renders one CronJob per enabled jira and slack job
    template: charts/declarative-agent/templates/scraper-cronjobs.yaml
    asserts:
      - hasDocuments:
          count: 3
  - it: jira CronJob runs jira_job module with mounted job config
    template: charts/declarative-agent/templates/scraper-cronjobs.yaml
    documentIndex: 0
    asserts:
      - isKind:
          of: CronJob
      - equal:
          path: spec.jobTemplate.spec.template.spec.containers[0].command[2]
          value: hosted_agents.scrapers.jira_job
      - contains:
          path: spec.jobTemplate.spec.template.spec.containers[0].env
          content:
            name: SCRAPER_JOB_CONFIG
            value: /config/job.json
  - it: slack_search CronJob runs slack_job module
    template: charts/declarative-agent/templates/scraper-cronjobs.yaml
    documentIndex: 1
    asserts:
      - equal:
          path: spec.jobTemplate.spec.template.spec.containers[0].command[2]
          value: hosted_agents.scrapers.slack_job
  - it: renders RAG deployment when a scraper job is enabled
    template: charts/declarative-agent/templates/rag-deployment.yaml
    asserts:
      - hasDocuments:
          count: 1
      - isKind:
          of: Deployment
      - equal:
          path: metadata.labels["app.kubernetes.io/component"]
          value: rag
  # Per-test values: below repeat the same paths (jira-only / slack-only); helm-unittest
  # has no shared values anchor for multiple it: blocks, so duplication is intentional.
  - it: jira-only values render one scraper ConfigMap # [DALC-REQ-HELM-UNITTEST-004]
    values:
      - ../../examples/with-scrapers/values.jira-only.yaml
    template: charts/declarative-agent/templates/scraper-job-configmaps.yaml
    asserts:
      - hasDocuments:
          count: 1
  - it: jira-only single CronJob runs jira_job module
    values:
      - ../../examples/with-scrapers/values.jira-only.yaml
    template: charts/declarative-agent/templates/scraper-cronjobs.yaml
    documentIndex: 0
    asserts:
      - hasDocuments:
          count: 1
      - equal:
          path: spec.jobTemplate.spec.template.spec.containers[0].command[2]
          value: hosted_agents.scrapers.jira_job
  - it: jira-only values render RAG deployment
    values:
      - ../../examples/with-scrapers/values.jira-only.yaml
    template: charts/declarative-agent/templates/rag-deployment.yaml
    asserts:
      - hasDocuments:
          count: 1
  - it: slack-only values render one scraper ConfigMap # [DALC-REQ-HELM-UNITTEST-004]
    values:
      - ../../examples/with-scrapers/values.slack-only.yaml
    template: charts/declarative-agent/templates/scraper-job-configmaps.yaml
    asserts:
      - hasDocuments:
          count: 1
  - it: slack-only single CronJob runs slack_job module
    values:
      - ../../examples/with-scrapers/values.slack-only.yaml
    template: charts/declarative-agent/templates/scraper-cronjobs.yaml
    documentIndex: 0
    asserts:
      - hasDocuments:
          count: 1
      - equal:
          path: spec.jobTemplate.spec.template.spec.containers[0].command[2]
          value: hosted_agents.scrapers.slack_job
  - it: slack-only values render RAG deployment
    values:
      - ../../examples/with-scrapers/values.slack-only.yaml
    template: charts/declarative-agent/templates/rag-deployment.yaml
    asserts:
      - hasDocuments:
          count: 1
  - it: scraper jobs do not inherit postgres DSN when cursor backend is file [DALC-REQ-SCRAPER-CURSOR-003]
    template: charts/declarative-agent/templates/scraper-cronjobs.yaml
    set:
      declarative-agent:
        checkpoints:
          postgresUrl: postgres://shared.example/db
    documentIndex: 0
    asserts:
      - notContains:
          path: spec.jobTemplate.spec.template.spec.containers[0].env
          content:
            name: HOSTED_AGENT_POSTGRES_URL
      - notContains:
          path: spec.jobTemplate.spec.template.spec.containers[0].env
          content:
            name: SCRAPER_CURSOR_BACKEND
  - it: scraper jobs wire shared postgres DSN when cursor backend is postgres [DALC-REQ-SCRAPER-CURSOR-003]
    template: charts/declarative-agent/templates/scraper-cronjobs.yaml
    set:
      declarative-agent:
        checkpoints:
          postgresUrl: postgres://shared.example/db
        scrapers:
          cursorStore:
            backend: postgres
    documentIndex: 0
    asserts:
      - contains:
          path: spec.jobTemplate.spec.template.spec.containers[0].env
          content:
            name: SCRAPER_CURSOR_BACKEND
            value: postgres
      - contains:
          path: spec.jobTemplate.spec.template.spec.containers[0].env
          content:
            name: HOSTED_AGENT_POSTGRES_URL
            value: postgres://shared.example/db
  - it: scraper jobs prefer DSN Secret override for postgres backend [DALC-REQ-SCRAPER-CURSOR-004]
    template: charts/declarative-agent/templates/scraper-cronjobs.yaml
    set:
      declarative-agent:
        checkpoints:
          postgresUrl: postgres://shared.example/db
        scrapers:
          cursorStore:
            backend: postgres
            postgresUrlSecretName: scraper-dsn
            postgresUrlSecretKey: url
    documentIndex: 0
    asserts:
      - contains:
          path: spec.jobTemplate.spec.template.spec.containers[0].env
          content:
            name: SCRAPER_POSTGRES_URL
            valueFrom:
              secretKeyRef:
                name: scraper-dsn
                key: url
      - notContains:
          path: spec.jobTemplate.spec.template.spec.containers[0].env
          content:
            name: HOSTED_AGENT_POSTGRES_URL
